/**
 * Preview feature flags shared across center-pane selection and preview rendering.
 * Keeping this centralized prevents mismatches where one layer disables a preview
 * but another layer still opens the pane.
 */
export const PLAIN_TEXT_FILE_PREVIEW_ENABLED = true;
